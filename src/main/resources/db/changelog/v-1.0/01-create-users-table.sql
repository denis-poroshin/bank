create table wallet(
    id serial primary key,
    amount integer,
    primary key (id)
);




-- CREATE TABLE  wallet (
--     id NOT NULL,
--     amount integer (150),
--     CONSTRAINT wallet_pkey PRIMARY KEY (id)
-- );